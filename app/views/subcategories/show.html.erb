<%= render :partial => 'partials/header_search_bar' %>

<div>
  <%= render :partial => 'categories/show', locals: {
    categories: @categories,
    category: @category,
    listings: @listings,
    subcategory: @subcategory,
  }  %>
</div>
