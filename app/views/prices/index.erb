<div class = "container">
    <header class="business-header">
        <div class="container">
            <div class="row">
                <div class="col-lg-12">
                </div>
            </div>
        </div>
    </header>

    <!-- Page Content -->
    <div class="container">

    Category - Sub Category - Prices (USD):<br />
    <% @chargable_sub_cat.each do |pricing| %>
        <%= pricing.category.name %> - <%= pricing.name %> - <%= number_to_currency(pricing.chargable/100) %><br />
    <% end%>


    </div>
</div>
<div style="height: 15px; width: 100%;"></div>
