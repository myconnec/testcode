/**
 * ConnecHub Core Example Tests
 *
 * Many methods provided by the JS libraries Mock and Chai
 *
 * Test phases:
 * Arrange - Setup Init application state
 * Act - Do some action
 * Assert - make an assertion
 *
 * "cy" is the global Cypress object
 */
describe('Test boolean true eval', function () {
    // basic boolean assertion
    it('Eval for true', function () {
        expect(true).to.equal(false)
    })
})

describe('Visit ConnecHub locally', function () {
    it('Ensurs the landing page loads', function () {
        // visit a page
        // find an element
        // interact with it
        // assert result
    })
})
